name: qiscus_chat_sample
description: Qiscus Chat SDK Sample Project
version: 1.0.3+alpha1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  qiscus_chat_sdk: 1.0.18
  cupertino_icons: ^1.0.3
  firebase_core: ^1.7.0
  firebase_messaging: ^10.0.8
  timeago: ^3.1.0
  provider: ^6.0.1
  file_picker: ^4.1.3
  date_format: ^2.0.4
  grouped_list: ^4.1.0
  flutter_downloader: ^1.7.1
  permission_handler: ^8.2.2
  path_provider: ^2.0.5
  emoji_picker_flutter: ^1.0.7

# useful for local development
# dependency_overrides:
#   qiscus_chat_sdk:
#     path: ../qiscus-chat-sdk-flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/wallpaper/
